{% extends 'base.html.twig' %}

{% block title %}RecipeProduct{% endblock %}

{% block body %}
    <h1>RecipeProduct</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recipe_product.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('recipe_product_index') }}">back to list</a>

    <a href="{{ path('recipe_product_edit', {'id': recipe_product.id}) }}">edit</a>

    {{ include('recipe_product/_delete_form.html.twig') }}
{% endblock %}
