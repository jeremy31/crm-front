<form method="post" action="{{ path('product_declension_delete', {'id': product_declension.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product_declension.id) }}">
    <button class="btn">Delete</button>
</form>
